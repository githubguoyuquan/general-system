# ========== 冷配置（修改后须重启 Next.js）==========
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/general_system?schema=public"
AUTH_SECRET="用 openssl rand -base64 32 生成"
AUTH_TRUST_HOST="true"
# OAuth 预留：填写后自动启用 Google 登录
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# LOG_LEVEL=debug

# ========== 热配置 ==========
# 网站名称、功能开关、是否开放注册等存于数据库 SystemConfig 表，
# 通过 /admin/settings 或 getSysConfig() 读取（支持缓存）。
