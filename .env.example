# ========== 冷配置（修改后须重启 Next.js）==========
# 本机跑 Next（make dev-host）：数据库在宿主机可达的地址（Docker 只起 db 时一般为 localhost:5432）
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/general_system?schema=public

# 使用「make dev」整栈时：compose 会给 app 容器注入 DATABASE_URL=...@db:5432...，不必改本行；
# 但若在容器外跑 prisma CLI，仍可用上面的 localhost（需映射 5432 端口）。

# 生成：openssl rand -base64 32
AUTH_SECRET=请替换为随机字符串
AUTH_TRUST_HOST=true

# 使用 docker-compose.prod.yml 时，数据库密码来自此处（compose 会读 .env）
POSTGRES_PASSWORD=请替换为强密码

# OAuth 预留：填写后自动启用 Google 登录
# AUTH_GOOGLE_ID=
# AUTH_GOOGLE_SECRET=
# LOG_LEVEL=debug

# ========== 热配置 ==========
# 网站名称、功能开关、是否开放注册等存于数据库 SystemConfig 表，
# 通过 /admin/settings 或 getSysConfig() 读取（支持缓存）。
